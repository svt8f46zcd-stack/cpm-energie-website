export type BillAnalysisField = { value: string | number | null; confidence: "high" | "medium" | "low" | "unknown"; source: "document" | "not_detected" };
export type BillAnalysisResult = { energyType: BillAnalysisField; provider: BillAnalysisField; annualConsumptionKwh: BillAnalysisField; workPriceCtPerKwh: BillAnalysisField; basePriceEurPerYear: BillAnalysisField; monthlyPaymentEur: BillAnalysisField; billingPeriod: BillAnalysisField; contractEnd: BillAnalysisField; cancellationPeriod: BillAnalysisField; address: BillAnalysisField };
export const BILL_ANALYSIS_FIELDS: Array<keyof BillAnalysisResult> = ["energyType","provider","annualConsumptionKwh","workPriceCtPerKwh","basePriceEurPerYear","monthlyPaymentEur","billingPeriod","contractEnd","cancellationPeriod","address"];
export function emptyBillAnalysis(): BillAnalysisResult { const e=():BillAnalysisField=>({value:null,confidence:"unknown",source:"not_detected"}); return {energyType:e(),provider:e(),annualConsumptionKwh:e(),workPriceCtPerKwh:e(),basePriceEurPerYear:e(),monthlyPaymentEur:e(),billingPeriod:e(),contractEnd:e(),cancellationPeriod:e(),address:e()}; }
declare global { interface Window { Tesseract?: any; } }
function loadScript(src:string):Promise<void>{return new Promise((resolve,reject)=>{if(document.querySelector(`script[data-cpm-src="${src}"]`))return resolve();const s=document.createElement("script");s.src=src;s.async=true;s.dataset.cpmSrc=src;s.onload=()=>resolve();s.onerror=()=>reject(new Error("OCR_LIBRARY_LOAD_FAILED"));document.head.appendChild(s);});}
function preprocessImage(source:unknown,variant:number):HTMLCanvasElement|unknown{if(typeof document==="undefined")return source;const c=document.createElement("canvas"),img=document.createElement("canvas");if(source instanceof HTMLCanvasElement){img.width=source.width;img.height=source.height;img.getContext("2d")!.drawImage(source,0,0);}else if(source instanceof HTMLImageElement){img.width=source.naturalWidth||source.width;img.height=source.naturalHeight||source.height;img.getContext("2d")!.drawImage(source,0,0);}else{return source;}const scale=variant===0?1.5:2;c.width=Math.ceil(img.width*scale);c.height=Math.ceil(img.height*scale);const ctx=c.getContext("2d");if(!ctx)return source;ctx.imageSmoothingEnabled=false;ctx.drawImage(img,0,0,c.width,c.height);if(variant===1||variant===2){const data=ctx.getImageData(0,0,c.width,c.height),d=data.data;for(let i=0;i<d.length;i+=4){const lum=0.299*d[i]+0.587*d[i+1]+0.114*d[i+2];const v=variant===1?Math.max(0,Math.min(255,(lum-128)*1.8+128)):lum<190?0:255;d[i]=d[i+1]=d[i+2]=v;}ctx.putImageData(data,0,0);}return c;}
async function ocrImage(image:unknown):Promise<string>{await loadScript("https://cdn.jsdelivr.net/npm/tesseract.js@5.1.1/dist/tesseract.min.js");if(!window.Tesseract)throw new Error("OCR_LIBRARY_LOAD_FAILED");const w=await window.Tesseract.createWorker("deu");try{const out:string[]=[];for(const variant of [0,1,2]){const prepared=preprocessImage(image,variant);for(const mode of ["6","11"]){await w.setParameters({tessedit_pageseg_mode:mode,preserve_interword_spaces:"1",user_defined_dpi:"300"});const x=await w.recognize(prepared);if(x.data.text)out.push(x.data.text);}}return out.join("\n");}finally{await w.terminate();}}
async function extractText(file:File):Promise<string>{if(file.type!=="application/pdf")return ocrImage(file);const importer=new Function("url","return import(url)") as (url:string)=>Promise<any>;const pdfjs=await importer("https://cdn.jsdelivr.net/npm/pdfjs-dist@5.4.54/build/pdf.mjs");const pdf=await pdfjs.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise;const pages=Math.min(pdf.numPages,8);const chunks:string[]=[];for(let i=1;i<=pages;i++){const p=await pdf.getPage(i),v=p.getViewport({scale:3.2}),c=document.createElement("canvas");c.width=Math.ceil(v.width);c.height=Math.ceil(v.height);const ctx=c.getContext("2d");if(!ctx)continue;await p.render({canvasContext:ctx,viewport:v}).promise;chunks.push(await ocrImage(c));}return chunks.join("\n");}
function normalizeOcr(t:string):string{return t.replace(/\u00a0/g," ").replace(/\r/g,"").replace(/[|]/g," ").replace(/[€]/g," € ").replace(/[«»]/g,'"').replace(/\bCts?\b/gi,"ct").replace(/kwh/gi,"kWh").replace(/\s+/g," ");}
function deNumber(value:string,context?:"currency"|"consumption"|"price"):number|null{let s=value.replace(/\s/g,"").replace(/€/g,"").replace(/[^0-9,.-]/g,"");if(!s)return null;if(context==="currency"&&/^\d{4,5}$/.test(s)){const n=Number(s);if(n>=1000&&n<=999999)return n/100;}if(s.includes(",")){const n=Number(s.replace(/\./g,"").replace(",","."));return Number.isFinite(n)?n:null;}if(context==="consumption"&&/^\d{1,3}(?:\.\d{3})+$/.test(s)){const n=Number(s.replace(/\./g,""));return Number.isFinite(n)?n:null;}const n=Number(s.replace(/,/g,""));return Number.isFinite(n)?n:null;}
function makeField(value:string|number|null,confidence:BillAnalysisField["confidence"]="medium"):BillAnalysisField{return value===null||value===""?{value:null,confidence:"unknown",source:"not_detected"}:{value,confidence,source:"document"};}
function findNumber(text:string,patterns:Array<{pattern:RegExp;context?:"currency"|"consumption"|"price"}>):number|null{for(const {pattern,context} of patterns){const m=text.match(pattern);if(m?.[1]){const n=deNumber(m[1],context);if(n!==null)return n;}}return null;}
function firstDateRange(t:string):string|null{const m=t.match(/(\d{1,2}[.\/]\d{1,2}[.\/]\d{4})\s*(?:-|–|bis)\s*(\d{1,2}[.\/]\d{1,2}[.\/]\d{4})/i);return m?`${m[1].replace(/\//g,".")} - ${m[2].replace(/\//g,".")}`:null;}
function parseAnnualConsumption(clean:string,normalized:string):BillAnalysisField{const patterns=[/(?:jahresverbrauch|jahresverbrauchs?wert)[^\d]{0,320}(\d{1,3}(?:[.]\d{3})+(?:,\d+)?|\d{4,6}(?:,\d+)?)\s*kwh/i,/(?:auf|für)\s+365\s*tage[^\d]{0,320}(\d{1,3}(?:[.]\d{3})+(?:,\d+)?|\d{4,6}(?:,\d+)?)\s*kwh/i,/(?:jahresverbrauch|365\s*tage)[\s\S]{0,500}?\b(\d{1,3}(?:[.]\d{3})+(?:,\d+)?|\d{4,6}(?:,\d+)?)\b\s*kwh/i];for(const p of patterns){const m=clean.match(p);if(m?.[1]){const n=deNumber(m[1],"consumption");if(n!==null&&n>=500&&n<=100000)return makeField(n,"high");}}const marker=normalized.search(/jahresverbrauch\s+in\s+kwh|auf\s+365\s+tage\s+umgerechnet|365\s+tage\s+umgerechnet/i);if(marker>=0){const c=normalized.slice(marker,marker+1200);const nums=[...c.matchAll(/\b(\d{1,3}(?:\.\d{3})+(?:,\d+)?|\d{4,6}(?:,\d+)?)\b\s*kwh/gi)].map(m=>deNumber(m[1],"consumption")).filter((n):n is number=>n!==null&&n>=500&&n<=100000);if(nums.length)return makeField(nums[0],"high");}return makeField(null);}
function parseText(text:string):BillAnalysisResult{const r=emptyBillAnalysis();const clean=text.replace(/\u00a0/g," ").replace(/\r/g,"");const n=normalizeOcr(clean);const hasStrom=/\bstrom\b/i.test(clean)||/\bstr[o0]m\b/i.test(clean)||/\bstromkosten\b/i.test(clean);const hasGas=/\bgas\b/i.test(clean)||/\bgaskosten\b/i.test(clean);r.energyType=makeField(hasStrom&&hasGas?"Strom + Gas":hasGas?"Gas":hasStrom?"Strom":null,hasStrom||hasGas?"high":"unknown");const provider=/\be\s*\.?\s*on\b/i.test(clean)||/eon/i.test(clean)?"E.ON":/\benbw\b/i.test(clean)?"EnBW":/\bvattenfall\b/i.test(clean)?"Vattenfall":/\bmainova\b/i.test(clean)?"Mainova":/\bewe\b/i.test(clean)?"EWE":null;r.provider=makeField(provider,provider?"high":"unknown");r.annualConsumptionKwh=parseAnnualConsumption(clean,n);r.workPriceCtPerKwh=makeField(findNumber(clean,[{pattern:/arbeitspreis[^\d]{0,180}(\d{1,3}(?:[.,]\d{1,3})?)\s*ct\s*\/?\s*kwh/i,context:"price"},{pattern:/(\d{1,3}(?:[.,]\d{1,3})?)\s*ct\s*\/?\s*kwh[^\n]{0,140}?arbeitspreis/i,context:"price"}]));r.basePriceEurPerYear=makeField(findNumber(clean,[{pattern:/grundpreis[^\d]{0,180}(\d{1,5}(?:[.,]\d{1,2})?)\s*€\s*\/?\s*(?:jahr|a)\b/i,context:"currency"},{pattern:/grundpreis[^\d]{0,180}(\d{1,5}(?:[.,]\d{1,2})?)\s*€/i,context:"currency"}]));r.monthlyPaymentEur=makeField(findNumber(clean,[{pattern:/(?:monatlicher|monatliche)\s+abschlag[^\d]{0,100}(\d{1,4}(?:[.,]\d{2})?)\s*€/i,context:"currency"},{pattern:/(?:aktueller|aktuelle)\s+abschlag[^\d]{0,100}(\d{1,4}(?:[.,]\d{2})?)\s*€/i,context:"currency"}]));const period=firstDateRange(clean);r.billingPeriod=makeField(period,period?"high":"unknown");const end=clean.match(/(?:vertragsende|vertragslaufzeit\s*bis|belieferung\s*bis)[\s:]+(\d{1,2}[.\/]\d{1,2}[\/]\d{4})/i);r.contractEnd=makeField(end?.[1]?.replace(/\//g,".")||null,end?"medium":"unknown");const cancel=clean.match(/k(?:ü|u)ndigungsfrist[\s:]+([^\n]{1,80})/i);r.cancellationPeriod=makeField(cancel?.[1]?.trim()||null,cancel?"medium":"unknown");const address=clean.match(/(?:anschrift|lieferanschrift|verbrauchsstelle)[\s:]+([^\n]{5,120})/i);r.address=makeField(address?.[1]?.trim()||null,address?"medium":"unknown");return r;}
export async function analyzeBill(file:File):Promise<BillAnalysisResult>{if(typeof window==="undefined")throw new Error("BILL_ANALYSIS_BROWSER_ONLY");const endpoint=process.env.NEXT_PUBLIC_BILL_ANALYSIS_URL;if(endpoint){const body=new FormData();body.append("file",file,file.name);const response=await fetch(endpoint,{method:"POST",body});if(!response.ok)throw new Error("BILL_ANALYSIS_FAILED");return(await response.json()) as BillAnalysisResult;}const text=await extractText(file);if(!text.trim())throw new Error("BILL_TEXT_NOT_RECOGNIZED");return parseText(text);}
