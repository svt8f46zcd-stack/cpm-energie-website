export type UniversalConsumption = number | null;
export type UniversalPrices = { workPriceCtPerKwh: number | null; basePriceEurPerYear: number | null };
function normalize(raw:string){return raw.replace(/\u00a0/g," ").replace(/[‐‑‒–—]/g,"-").replace(/\r/g,"\n").replace(/k\s*[wvw]\s*[hn]/gi,"kWh").replace(/kwh/gi,"kWh").replace(/kw\s*h/gi,"kWh").replace(/c\s*t\s*\/\s*k\s*w\s*h/gi,"ct/kWh").replace(/c\s*t\s*\/\s*kwh/gi,"ct/kWh").replace(/c[au]\s*\/?\s*kwh/gi,"ct/kWh").replace(/c\s*t\s*kwh/gi,"ct/kWh").replace(/grund[o0]reis|grundpre[i1]s|grundoeis|grundprels/gi,"Grundpreis").replace(/grundgeb[uü]hr|grundentgelt|bereitstellungspreis/gi,"Grundpreis").replace(/arbeit\s*(?:s|ss)?\s*pre[i1]s|arbeitspre[il]s|ar[bp]atspre[i1]s/gi,"Arbeitspreis").replace(/verbrauchspre[il]s|verbrauch\s*spre[i1]s/gi,"Verbrauchspreis").replace(/jahresstromverbrauch|jahresverbrauch/gi,"Jahresverbrauch").replace(/abg[o0]r[o0]chneter\s+verbrauch|abger[e3]chneter\s+verbrauch/gi,"Abgerechneter Verbrauch").replace(/sum[mn]e/gi,"Summe").replace(/gesamtverbrauch/gi,"Gesamtverbrauch").replace(/ihr\s+stromverbrauch/gi,"Ihr Stromverbrauch").replace(/ihr\s+verbrauch/gi,"Ihr Verbrauch").replace(/[ \t]+/g," ");}
function num(raw:string){let s=raw.replace(/\s/g,"").replace(/[^0-9,.+-]/g,"");if(!s)return null;const c=s.lastIndexOf(","),d=s.lastIndexOf(".");if(c>=0&&d>=0)s=c>d?s.replace(/\./g,"").replace(",","."):s.replace(/,/g,"");else if(c>=0)s=s.replace(/\./g,"").replace(",",".");else if(/^[-+]?\d{1,3}(?:\.\d{3})+$/.test(s))s=s.replace(/\./g,"");const n=Number(s);return Number.isFinite(n)?n:null;}
const PROVIDER_ALIASES:Record<string,RegExp>={EON:/\bE\.?\s*ON\b|Energie Deutschland/i,ENTEGA:/\bENTEGA\b/i,EnBW:/\bEnBW\b/i,Vattenfall:/\bVattenfall\b/i,Mainova:/\bMainova\b/i,SWM:/\bSWM\b|Stadtwerke München/i,Yello:/\bYello\b/i,eprimo:/\beprimo\b/i,EWE:/\bEWE\b/i,ESWE:/\bESWE\b/i,Süwag:/\bS[üu]wag\b/i,EWR:/\bEWR\b/i,MVV:/\bMVV\b/i,RheinEnergie:/\bRheinEnergie\b/i,Naturstrom:/\bNaturstrom\b/i,LichtBlick:/\bLichtBlick\b/i,Octopus:/\bOctopus\s*Energy\b/i,Tibber:/\bTibber\b/i,enercity:/\benercity\b/i,swb:/\bswb\b/i,StadtwerkeMünster:/\bStadtwerke\s+Münster\b/i,Stadtwerke:/\bStadtwerke\b/i,Polarstern:/\bPolarstern\b/i,GreenpeaceEnergy:/\bGreenpeace\s*Energy\b/i,Ostrom:/\bOstrom\b/i,Rabot:/\bRabot\b/i,immergrün:/\bimmergrün\b/i,Grünwelt:/\bGrünwelt\b/i,lekker:/\blekker\b/i,MEGA:/\bMEGA\b/i,NEW:/\bNEW\b/i,SWK:/\bSWK\b/i,StadtwerkeKöln:/\bStadtwerke\s*Köln\b/i,StadtwerkeDüsseldorf:/\bStadtwerke\s*Düsseldorf\b/i};
export function detectProvider(raw:string){for(const [name,re] of Object.entries(PROVIDER_ALIASES))if(re.test(raw))return name;return null;}
function candidateKwh(text:string){const out:{n:number;score:number;context:string}[]=[];const re=/(\d{1,3}(?:[.\s]\d{3})+(?:,\d+)?|\d{4,8}(?:,\d+)?|\d{1,3}(?:[,.]\d{1,3})?)\s*kWh\b/gi;for(const m of text.matchAll(re)){const n=num(m[1]);if(n===null||n<50||n>100000)continue;const i=m.index??0;const context=text.slice(Math.max(0,i-240),Math.min(text.length,i+240));let score=0;if(/Summe|Gesamtverbrauch|Gesamt\b/i.test(context))score+=110;if(/Ihr Verbrauch|Ihr Stromverbrauch|Abgerechneter Verbrauch|Jahresverbrauch|aktueller Verbrauch|aktuellen Verbrauch|Verbrauchsermittlung/i.test(context))score+=100;if(/Verbrauch in diesem Abrechnungszeitraum|Verbrauch dieses Abrechnungszeitraums/i.test(context))score+=100;if(/Verbrauchsvergleich|Vorjahr|letzten Abrechnungszeitraum|Jahresverbrauchsprognose|Prognose|Entlastungskontingent|Durchschnitt|Vergleich/i.test(context))score-=100;if(/kWh\/Tag|pro Tag/i.test(context))score-=60;if(/Stromsteuer|Umlage|Netzentgelt|Messstellenbetrieb/i.test(context))score-=50;if(n>50000)score-=80;if(n>=100&&n<=100000)score+=10;out.push({n,score,context});}return out;}
export function extractUniversalConsumption(raw:string):UniversalConsumption{const t=normalize(raw);const c=candidateKwh(t);if(!c.length)return null;const explicit=c.filter(x=>x.score>=110&&x.n<50000);if(explicit.length)return explicit.sort((a,b)=>b.score-a.score||b.n-a.n)[0].n;const strong=c.filter(x=>x.score>=100&&x.n<50000);if(strong.length)return strong.sort((a,b)=>b.score-a.score||b.n-a.n)[0].n;const sane=c.filter(x=>x.n<50000);return sane.length?sane.sort((a,b)=>b.score-a.score||b.n-a.n)[0].n:c.sort((a,b)=>b.score-a.score)[0].n;}
function priceNumber(raw:string){const n=num(raw);return n!==null&&n>=5&&n<=100?n:null;}
function baseNumber(raw:string){const n=num(raw);return n!==null&&n>=10&&n<=10000?n:null;}
export function extractUniversalPrices(raw:string):UniversalPrices{const t=normalize(raw);const works:{n:number;score:number}[]=[];const bases:{n:number;score:number}[]=[];for(const m of t.matchAll(/(Arbeitspreis|Verbrauchspreis)([\s\S]{0,420}?)(\d{1,3}[,.]\d{1,4})\s*(?:Cent|ct|c|cu|ca)\s*\/?\s*kWh/gi)){const n=priceNumber(m[3]);if(n===null)continue;const ctx=m[0];let score=100;if(/Stromsteuer|EEG|Umlage|Netzentgelt|Konzessionsabgabe|Referenzpreis|Entlastung|Mindestdurchschnittspreis/i.test(ctx))score-=90;if(/Ihr Strompreis|Ihre Preise|aktuell|gültig/i.test(ctx))score+=10;works.push({n,score});}for(const m of t.matchAll(/(\d{1,3}[,.]\d{1,4})\s*(?:Cent|ct|c|cu|ca)\s*\/?\s*kWh/gi)){const n=priceNumber(m[1]);if(n===null)continue;const ctx=t.slice(Math.max(0,(m.index??0)-180),Math.min(t.length,(m.index??0)+180));let score=20;if(/Arbeitspreis|Verbrauchspreis|Ihr Strompreis|Ihre Preise/i.test(ctx))score+=60;if(/Stromsteuer|EEG|Umlage|Netzentgelt|Konzessionsabgabe|Referenzpreis|Mindestdurchschnittspreis/i.test(ctx))score-=90;works.push({n,score});}for(const m of t.matchAll(/(Grundpreis|Grundgebühr|Grundentgelt|Bereitstellungspreis)([\s\S]{0,360}?)(\d{1,5}[,.]\d{1,2})\s*(?:€|EUR|E|Eu)?\s*(?:\/\s*)?(?:Jahr|jährlich|year|a)\b/gi)){const n=baseNumber(m[3]);if(n!==null)bases.push({n,score:100});}for(const m of t.matchAll(/(\d{1,5}[,.]\d{1,2})\s*(?:€|EUR|E|Eu)\s*\/\s*(?:Jahr|jährlich|year)/gi)){const n=baseNumber(m[1]);if(n!==null){const ctx=t.slice(Math.max(0,(m.index??0)-180),Math.min(t.length,(m.index??0)+180));let score=/Grundpreis|Grundgebühr|Grundentgelt|Bereitstellungspreis/i.test(ctx)?90:20;if(/Zahlung|Abschlag|Rechnungsbetrag|Gutschrift/i.test(ctx))score-=80;bases.push({n,score});}}for(const m of t.matchAll(/(Grundpreis|Grundgebühr|Grundentgelt|Bereitstellungspreis)([\s\S]{0,180}?)(\d{1,4}[,.]\d{1,2})\s*(?:€|EUR|E|Eu)\s*\/?\s*(?:Monat|monatlich|month)/gi)){const n=baseNumber(m[3]);if(n!==null)bases.push({n:n*12,score:95});}works.sort((a,b)=>b.score-a.score||b.n-a.n);bases.sort((a,b)=>b.score-a.score||b.n-a.n);return{workPriceCtPerKwh:works.length?works[0].n:null,basePriceEurPerYear:bases.length?bases[0].n:null};}
export const UNIVERSAL_INVOICE_FORMATS=["E.ON","ENTEGA","EnBW","Vattenfall","Mainova","SWM","Yello","eprimo","EWE","ESWE","Süwag","EWR","MVV","RheinEnergie","Naturstrom","LichtBlick","Octopus Energy","Tibber","enercity","swb","Stadtwerke Münster","Stadtwerke","Polarstern","Greenpeace Energy","Ostrom","Rabot Energy","immergrün","Grünwelt","lekker","MEGA","NEW","SWK","regionale Stadtwerke","Grundversorger","Gasversorger"];